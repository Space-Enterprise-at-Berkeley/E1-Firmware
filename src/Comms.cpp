#include <Comms.h>

namespace Comms {
    uint32_t packetWaiting() {
        return 0; // return 1 if packets waiting
    }

    void processPackets() {
        // process all waiting packets
    }
};
